spring:
  cloud:
    azure:
      credential:
        managed-identity-enabled: true
      storage:
        blob:
          account-name: nuridevlog
          container-name: nuri
          endpoint: https://nuridevlog.blob.core.windows.net/
          